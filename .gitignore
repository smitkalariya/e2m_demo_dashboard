# Root (docker compose env overrides)
/.env

# Vercel CLI project link (local-only)
/.vercel

# Backend (Python)
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.pytest_cache/
backend/.ruff_cache/
backend/.env
backend/*.db
backend/.coverage

# Frontend (Next.js) — frontend/.gitignore covers node_modules/.next/etc.

# Editors / OS
.vscode/
.DS_Store
Thumbs.db
